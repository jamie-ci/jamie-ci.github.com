!!!
%html
  %head
    %meta{:charset => 'utf-8'}

    / Always force latest IE rendering engine or request Chrome Frame
    %meta{:content => 'IE=edge,chrome=1', :'http-equiv' => 'X-UA-Compatible'}

    / Use title if it's in the page YAML frontmatter
    %title= data.page.title ? "#{data.page.title} | Jamie CI" : 'Jamie CI'

    = stylesheet_link_tag 'normalize', 'application'
    = javascript_include_tag  'application'

    %body
      %header#header
        .wrap
          %span#logo Jamie CI

      %section#main
        .wrap
          %section#sidebar
            %h3 Guides
            %ul
              - sitemap.resources.select { |resource| resource.destination_path =~ /^guides\/.+/ }.each do |resource|
                %li
                  = link_to resource.metadata[:page]['title'], resource

          #content
            = yield
